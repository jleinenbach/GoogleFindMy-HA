{
  "config": {
    "step": {
      "user": {
        "title": "Google Find My Device Authentication",
        "description": "Run GoogleFindMyTools on a computer with Chrome to generate authentication tokens, then provide the secrets.json file contents.",
        "data": {
          "auth_method": "Authentication Method"
        }
      },
      "secrets_json": {
        "title": "GoogleFindMyTools secrets.json",
        "description": "Run GoogleFindMyTools on a machine with Chrome to generate authentication tokens, then paste the complete contents of the Auth/secrets.json file below.",
        "data": {
          "secrets_json": "Contents of secrets.json file"
        }
      }
    },
    "error": {
      "auth_failed": "Authentication failed. Please try again.",
      "invalid_token": "Invalid token provided or missing required fields.",
      "invalid_json": "Invalid JSON format in secrets.json content.",
      "unknown": "Unexpected error occurred."
    },
    "abort": {
      "already_configured": "Google Find My Device is already configured.",
      "cannot_connect": "Failed to connect to Google Find My Device."
    }
  },
  "services": {
    "locate_device": {
      "name": "Locate Device",
      "description": "Get the current location of a Google Find My device.",
      "fields": {
        "device_id": {
          "name": "Device ID",
          "description": "The ID of the device to locate."
        }
      }
    },
    "play_sound": {
      "name": "Play Sound",
      "description": "Play a sound on a Google Find My device.",
      "fields": {
        "device_id": {
          "name": "Device ID",
          "description": "The ID of the device to play sound on."
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Google Find My Device Options",
        "description": "Configure tracking and staleness settings for Google Find My Device integration.",
        "data": {
          "tracked_devices": "Tracked Devices",
          "location_poll_interval": "Location Poll Interval (seconds)",
          "device_poll_delay": "Device Poll Delay (seconds)",
          "min_accuracy_threshold": "Minimum Accuracy Threshold (meters)",
          "staleness_threshold_hours": "Staleness Threshold (hours, 0.5-24)"
        }
      }
    }
  }
}