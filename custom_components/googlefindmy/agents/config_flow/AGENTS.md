# custom_components/googlefindmy/agents/config_flow/AGENTS.md

## Scope

Runtime + config flow guidance for every module under `custom_components/googlefindmy/`. Apply these instructions whenever
flows, service schemas, or reconfigure hooks are updated.

## Config flow registration expectations

* Keep `ConfigFlow.domain` explicitly declared in `config_flow.py`. This guards against future upstream changes that might stop injecting the attribute via metaclass magic.
* Never hand-register the flow through `config_entries.HANDLERS` unless Home Assistant drops automatic registration. If a regression forces a manual fallback, document the affected core versions, link the upstream issue, and add a TODO describing when to remove the workaround.
* Tests under `tests/test_config_flow_registration.py` cover both the domain attribute and automatic handler registration. Update them whenever the runtime behavior changes so the expectations stay enforced.
* Reference the Home Assistant developer docs on [config flow registries and handlers](https://developers.home-assistant.io/docs/config_entries_config_flow_handler/#config-flow-handler-registration) when validating upstream behavior; keep this section aligned with any future changes noted there.
* When config flows iterate existing entries, guard optional Home Assistant attributes (for example, `ConfigEntry.source`) so discovery update stubs and other test doubles without those attributes keep working during local runs.
* Type-check the **frozen** entry attributes — `ConfigEntry.subentries`, `.data`, `.options` and a `ConfigSubentry.data` — against `collections.abc.Mapping`, never against `dict`. The core hands all of them out as `MappingProxyType`, so a `dict` guard is false for every real entry and true only for the mutable test doubles: it does not fail, it silently skips. `_gather_subentry_options` carried such a guard from 2025-10-27 until the fix; the effect was not an error message but a subentry selection reduced to the synthetic fallback option, so no assignment could persist while the suite stayed green (`tests/AGENTS.md` now asks entry doubles to expose the read-only shape for the same reason). The rule is deliberately scoped to those attributes and is **not** a blanket ban on `dict` guards: `data_entry_flow.FlowContext` is a `TypedDict` and therefore a real `dict` at runtime, which is why the `isinstance(context, dict)` guards in this module are correct and must stay — several of them mutate the context afterwards (`setdefault`, `pop`, item assignment), which a `Mapping` would not permit.

### Integration module imports

* When a config flow needs helpers from the integration package, import the module via `importlib.import_module(__package__ or DOMAIN)` rather than dereferencing `__package__` attributes directly. Home Assistant may start flows with a `None` package hint, and relying on `__init__` attributes can regress when the package layout changes. Centralize this pattern so helper lookups stay robust across runtime and test stubs.
* Prefer the shared lazy import helpers in `custom_components/googlefindmy/integration_modules.py` (`import_integration_package`, `import_integration_api_module`) so the API and package modules stay lazily loaded while keeping import targets centralized for future changes.

### Subentry alias handling

* Canonical service and tracker group keys now normalize legacy labels (for example, stray email-style identifiers) through the alias-aware subentry manager. When reconciling discovery or reconfigure payloads, prefer the canonical keys surfaced by the manager over any stored group labels so collisions realign to the correct service/tracker groups instead of amplifying drift.
* **The service and hub groups never carry `visible_device_ids`, and a device target is decided by a predicate, not by a bare key comparison.** Three production sites already write the invariant that way: `ServiceSubentryFlowHandler._visible_device_ids` and `HubSubentryFlowHandler._visible_device_ids` both return `()` unconditionally (unlike the `_BaseSubentryFlow` version they override, which reads the stored value), and `_async_sync_feature_subentries` builds its `service_payload` through `_build_subentry_payload` **without** the `visible_device_ids` argument while `tracker_payload` passes the stored ids. `tests/test_config_flow_subentry_sync.py` pins that result. **That omission also deletes, and the deletion is load-bearing rather than incidental.** Because the core replaces `data` wholesale, a payload without the key clears whatever the service group had stored -- and that is the repair which keeps a wrong assignment written by the pre-`e8114585` flow from becoming permanent, measured by `test_service_subentry_visibility_is_cleared_by_the_setup_roundtrip` on the manager side. Read together with `_accepts_device_assignment`, which refuses such a target, no *new* service assignment can be created, so everything the deletion removes is legacy residue. Anyone tempted to "preserve" it because the tracker branch preserves its own list should reach this sentence first: the two branches differ because the groups differ, and the tracker one is a device group. The reading side is a fourth, separately tracked enforcement (see below), and `ConfigEntrySubEntryManager.update_visible_device_ids` a fifth; a test asserts, it does not write. None of the three writers was an authoritative checkpoint, which is why `_async_assign_devices_to_subentry` could store device ids on the service group anyway. It now refuses such a target, and the three steps that assign devices (`async_step_visibility`, `async_step_repairs_move`, and the `_FIELD_REPAIR_FALLBACK` field of `async_step_repairs_delete`) build their choices from `_device_target_choice_map` instead of `_subentry_choice_map`. The two `_resolve_existing` implementations are deliberately not in that count: they are a separate axis, enumerated below, and they decide *which* subentry answers for a key rather than whether a group may hold devices at all.
  * **Scope of that checkpoint, stated because an earlier version of this paragraph overstated it.** `ConfigEntrySubEntryManager.update_visible_device_ids` (`__init__.py`) is a second writer of `visible_device_ids`, outside this module. It is no longer outside a guard: it now refuses to write to a subentry whose `subentry_type` is in `NON_DEVICE_SUBENTRY_TYPES`, the writing-side mirror of `_accepts_device_assignment`. That check reads the **resolved subentry**, not the key, so it holds whichever key a caller passes and whatever the alias table makes of it; today the only production caller already folds such subentries away, which makes this a second barrier rather than the only one, and that is the point, because a future caller would otherwise be unguarded. The alias table it resolves through no longer answers with a foreign group either: a **core key never aliases at all** (`core_tracking` and `service` each already name a group of their own, so a `service`-typed subentry still storing `core_tracking` can no longer redirect every tracker write onto itself), and a key that **two subentries of different canonical identities claim resolves to nothing** rather than to whichever the entry yielded first. The second half only works because a subentry whose canonical identity *is* its stored key registers that claim as well: a `hub`-typed subentry (`SUBENTRY_TYPE_HUB`, a type this integration really does create) canonicalises onto its own key and registers no redirection, so counting redirections alone would have left it uncontested and let a service twin capture the key. The ambiguity is remembered rather than merely dropped, so a third subentry cannot re-establish the mapping later in the same pass. Its caller in `coordinator/subentry.py::_refresh_subentry_index` no longer depends on the key axis either, see the canonicalisation note below. Measured, not read: both iteration orders of the service/tracker pair are pinned for the core key and for a legacy key, together with the `hub` axis and the reserved service key, in `tests/test_subentry_manager_registry_resolution.py`.
  * **The second writer inside this module, `_async_sync_feature_subentries`, now carries the type axis too.** Its `service_payload` omission (above) protects the *payload*, not the *target*, and its local `_resolve_existing` used to resolve on the stored `group_key` alone: the fallback scan matched it directly, the first branch trusted whatever the seeded context map held. A `service`- or `hub`-typed subentry that still stores `core_tracking` was therefore resolved *as* the tracker group and received `tracker_payload`, and the `if not tracker_visible and self._available_devices` fallback made that worse than a pass-through by filling a previously clean twin with every probed device id. The mirror direction lost data instead of adding it: a `tracker`-typed subentry storing the *service* key was resolved as the service group, received the id-less `service_payload`, and the update dropped the assignment it held. Both directions were measured against the real helper, and Codex reported the same defect independently. What replaced it, at `config_flow.py` `_canonical_core_key_of` and the module-level `_may_answer_for` beside it (module level rather than nested since `_BaseSubentryFlow._resolve_existing` became its second caller):
    * A candidate's **type** names the core key it may answer for, and **the two directions are not symmetric**. Every type in `NON_DEVICE_SUBENTRY_TYPES` folds onto `SERVICE_SUBENTRY_KEY` unconditionally, because that is the only key such a type may ever answer for. `SUBENTRY_TYPE_TRACKER` folds onto `TRACKER_SUBENTRY_KEY` **only when it stores `SERVICE_SUBENTRY_KEY`**, because several tracker groups with distinct keys are a supported shape that `coordinator/subentry.py` states and honours. A first version folded every tracker unconditionally and thereby adopted a legacy per-account group (`group_key = "owner@example.com"`), overwriting its key, title and identity: the axis meant to prevent a data defect producing one. A candidate whose type says nothing (absent, or outside that pair) and a tracker on its own key keep being decided by their stored key, which is the pre-existing convention of `_accepts_device_assignment` and the reason untyped doubles still resolve.
    * Inside this module the comparison against the set lives in **one** place, `_subentry_type_accepts_devices`, which both `_accepts_device_assignment` and `_canonical_core_key_of` call. Scoped to this module deliberately: `const.py` enumerates the consumers repo-wide (now five, since the feature sync joined them) and each of the others owns its own `in NON_DEVICE_SUBENTRY_TYPES` test. What the shared set prevents is a second *definition*, not a second reader.
    * The axis guards **both** branches, the seeded one and the fallback scan, because the migration path only ever reaches the second while the reconfigure path decides in the first. Each half has its own killing mutation, so neither is decorative.
    * An **exact stored-key match wins over a folded twin**. Where a real service subentry and a mis-keyed twin both answer for the service group, the exact match is taken and the twin is left alone, which avoids rewriting a stored identity nobody asked to change. That preference alone does **not** settle the order question, because two candidates can reach the *same* exit: two folded twins with no exact match, or a `service`- and a `hub`-typed subentry both storing the service key, a shape this module produces itself (`HubSubentryFlowHandler._group_key` is `SERVICE_SUBENTRY_KEY`). Measured, taking whichever came first turned an `AbortFlow` at `b75bea42` into a silent `async_remove_subentry` of the **canonical service group**, registry bindings included: exactly the trade `_claim_unique_id` exists to prevent, in the opposite direction. Both exits are therefore ranked, and the rank is substantive before it is stable: the type that *literally* owns the key (`_LITERAL_CORE_KEY_OWNER`) beats one that merely folds onto it, because the entity platforms match `subentry_type == "service"` literally; only among equals does the lowest `subentry_id` decide, where the value is arbitrary and stability is the whole requirement. The **seed is a tie-break inside that rank, not a precedence in front of it**: the seeder resolves through the runtime manager, which folds every tracker-typed subentry onto `TRACKER_SUBENTRY_KEY`, so it can name a subentry that carries the key in neither pool -- a hub beside the canonical service subentry, or a legacy per-account tracker group beside the canonical core group. Ranking such a seed ahead of the pool produced the same silent deletion one level up (measured: `AbortFlow` at `b75bea42`, canonical group swept afterwards). A seed therefore only decides among candidates that qualify for the key, and decides alone only when none does. No `-legacy` displacement is involved in any of this: `_claim_unique_id` only renames a subentry holding the *desired* unique id, and a losing twin on its own id holds neither.
    * The **reconfigure seeding** in `_reset_reconfigure_subentry_context` deliberately does *not* call this helper. Rewriting it was tried and reverted; the last bullet of this section records the measurement that decided it.
  * **Folding renames a `hub` the user created, and that is deliberate.** A `hub`-typed subentry is written with `service_payload`, the service title `Google Find Hub Service` and the service `unique_id` whatever key it stores, not only `core_tracking` but an email-style key or none at all, because the reading side (`coordinator/subentry.py`) folds exactly those three shapes the same way and pins all three, and the offering side (`_accepts_device_assignment`) refuses them all on the type axis; leaving the writing side on a third semantics is the drift the shared set forbids. Unlike a tracker, a hub on its own key is **not** a legitimate separate group: there is one service group, whereas several tracker groups with distinct keys are supported, and that asymmetry is the whole reason the two directions of the fold differ. The user-visible cost is the title, plus any `visible_device_ids` the subentry had accumulated, which are write-back residue by the same argument the reading side makes and are reclaimed by the unassigned-device merge rather than kept. The alternative considered and rejected was to leave such a subentry out of both core keys, which creates a third group beside it and raises the collision rate. **That paragraph describes the hub that *wins* the service slot, and the qualifier is not pedantry:** where a real `service`-typed subentry **storing the service key** sits beside it, the literal-owner rank hands the slot to that subentry and the hub is neither written nor renamed. The stored-key half of that condition is load-bearing and was overstated here once: the rank is compared *inside* `pool = exact or folded`, so a service subentry that only *folds* onto the key loses to a hub that stores it exactly, which `::test_an_exact_stored_key_match_beats_a_folded_literal_owner` pins. A hub that loses the slot is then not a cheaper outcome but a different one, a group of its own that keeps its title, its key and its stored ids, and the cleanup must leave it alone (see below); its `unique_id` is untouched by the rank, and where it happened to hold the *desired* id it was already displaced by `_claim_unique_id` before any of this. Reading the fold as unconditional is what made the delete path look safe. **One consequence, measured rather than argued:** where a lone `hub` is adopted into the service slot, the entry afterwards holds a `hub` and a `tracker` group and **no `service`-typed** subentry, so `known_ids_for_subentry_type(entry, SUBENTRY_TYPE_SERVICE)` is empty and `ensure_config_subentry_id` falls into its `if valid_ids` pass-through, i.e. it stops verifying rather than breaking. That is a shifted gap, not a new one: at `b75bea42` the same entry came out as a `hub` and a `service` group with **no `tracker`** type, and with the probed devices on the non-device twin. The fold trades an unverified platform lookup for a data defect; closing it means giving the hub the service type, which is a migration and belongs to `PLAN_GFMY_SUBENTRY_TYPE_MIGRATION`, not here; it is carried as `B12` in the register below, with the reason the core offers no in-place route.
  * **The axis turns an update into a create, so the create had to be made collision-proof.** `ConfigEntries.async_add_subentry` calls `_raise_if_subentry_unique_id_exists` unconditionally and `async_update_subentry` calls it whenever the `unique_id` actually changes; both raise `AbortFlow("already_configured")`. A twin that is left alone keeps `{entry_id}-core_tracking`, which is the very id the tracker group is then created under, and the reconfigure entry point has no `try` around the sync, so an unhandled collision would trade a data defect for a dead flow. `_claim_unique_id` displaces the **legacy holder** instead, never the canonical group, and searches until the substitute is free for the same reason `_unclaimed_fallback_key` does. That direction is not a preference: five sites outside this flow build their subentry definitions with `f"{entry_id}-{key}"` and reconcile against it (`__init__.py` at three, `coordinator/subentry.py` at two), so a core group carrying a derived id would collide with the runtime manager on the next sync and be resolved by its type-blind adoption path. Pinned by `tests/test_config_flow_subentry_sync.py::test_folding_a_twin_does_not_abort_on_the_unique_id_it_leaves_behind`, which asserts both halves: the canonical id on the tracker group and the `-legacy` suffix on the twin.
  * **Leaving a twin alone re-opened a delete path, so the cleanup needed the axis too.** `_async_cleanup_stale_subentries` classifies by stored `group_key` alone, and `ConfigEntries.async_remove_subentry` clears the device and entity registry bindings of whatever it removes. Before the resolver read `subentry_type`, a mis-keyed twin was always resolved by its stored key and therefore always landed in `context_map`, which kept it out of the stale list **by accident**. Once a real service subentry can win the exact-key match, that accident is gone: the twin is neither written nor seeded, and it matched `group_key in allowed_keys`. The cleanup now skips a subentry unless its type **literally owns** the key it stores (`_LITERAL_CORE_KEY_OWNER`), and that phrasing is the load-bearing part: the narrower rule "the type names a *different* core key" leaves a second victim behind. A `hub` writes `SERVICE_SUBENTRY_KEY` by design, so beside a real service subentry it is not mis-keyed at all, yet the literal-owner rank makes it the **deterministic** loser of the resolver, where taking whichever came first used to leave the outcome open. Classifying it by its stored key alone therefore traded one silent deletion for another, with the hub as the victim instead of the service group. An unknown key has no known owner and is skipped for the same reason: removal is irreversible, skipping is not. Only the literal owner can be a leftover copy of a core group; every other type sitting on the key is a group of its own. **An untyped subentry is deliberately not covered**, and that exclusion is a decision rather than an oversight: `_canonical_core_key_of` treats a missing type as "the stored key keeps deciding", so such a subentry *is* a candidate copy of the core group it stores, and sweeping stale legacy copies is the whole purpose of this pass. How reachable that shape is, is deliberately *not* claimed here: `ConfigSubentry.subentry_type` is a required keyword-only `str` in the installed core, so a missing type is not a shape the core's own dataclass produces, and the branch is defensive rather than a migration path. It is kept because widening the guard to "anything whose type does not literally own the key" would put every unknown or future type out of reach of the sweep, and the guard is what decides between skipping and an irreversible removal. What the diff does change for it is only *which* of two untyped siblings survives, not *whether* one dies, which is unchanged from `b75bea42`. Pinned by `::test_a_left_alone_twin_is_not_swept_up_as_a_stale_core_group` (mis-keyed twin), `::test_a_hub_losing_the_service_slot_is_not_swept_up_either` (same key, lost slot) and, in the opposite direction, `::test_a_genuinely_orphaned_core_group_is_still_removed`. The middle one is the only assurance the narrower rule fails, measured by restoring it. **The third non-owner combination is pinned too, and it is the one that needs a third subentry to exist at all:** a `tracker`-typed subentry storing `SERVICE_SUBENTRY_KEY`. Beside a real service subentry *alone* it is adopted into the tracker slot and re-keyed to `TRACKER_SUBENTRY_KEY`, so it never meets this pass on the service key; only once a canonical `tracker`/`TRACKER_SUBENTRY_KEY` sibling wins that slot does it stay behind on a key it does not own. Skipping it follows from the same ownership rule, and its assurance is a mutation of its own: narrowing the guard to `subentry_type == SUBENTRY_TYPE_HUB` kills `::test_a_tracker_parked_on_the_service_key_is_not_swept_up` in both entry paths and nothing else. **What survives is a parked subentry, not a clean one**, and the difference is named here rather than left to be rediscovered: the reading side (`coordinator/subentry.py::_refresh_subentry_index`) used to fold by *stored* key for every type outside `NON_DEVICE_SUBENTRY_TYPES`, so such a subentry kept being indexed under the service key and the branch a few lines down forced its visible, enabled and manager ids to `()`. PR #1230 closed that: a `tracker` storing the service key is now folded onto `TRACKER_SUBENTRY_KEY` on the reading side too, so it no longer reaches the service key at all (measured in both iteration orders, `::test_ap4_a_parked_tracker_reaches_exactly_one_group`). **What does *not* follow is a manager write-back, and saying it did was an overstatement in an earlier version of this sentence.** `manager_visible` is only filled for keys other than the service key, and the sole production caller of `ConfigEntrySubEntryManager.update_visible_device_ids` (`coordinator/subentry.py`, in the `if manager and manager_visible` loop) skips that key again before calling, so the type guard inside the manager is never even reached on this path. What used to follow was quieter and is the reason the follow-up existed: the ids the parked subentry stored kept counting into `stored_assigned_ids`, so the unassigned-device merge did not reclaim them, while the service branch had already emptied the metadata ids -- the devices ended up described by no group. That is fixed rather than merely described now. The reading side folds such a subentry onto the tracker key **and** exempts its ids from the in-memory assignment bookkeeping, exactly as the mis-keyed fold does, so the merge puts the devices in the tracker group. Only from the bookkeeping: the group keeps its allow-list, because an absent one would let it see every device rather than none. The two folds differ in what they are keyed on and that difference is load-bearing: this one is the **type** axis, so `test_a_device_moved_to_the_service_subentry_is_left_alone` is untouched -- the subentry there is `service`-typed on the canonical key, so a deliberate user move onto the service group is still not reclaimed. Measured, not argued: with a canonical tracker sibling present the fold alone left the devices in no group, because the parked subentry loses the core-key rank; exempting the ids from the assignment bookkeeping is what completes it. The read side now shares both the *rank* and this *fold* with this module. What it still does not share is the manager's unconditional tracker fold, so a legacy per-account tracker key stays a group of its own here. What the diff changes for this shape is *reachability*, not that behaviour: at `b75bea42` the three-way collision was resolved by removing one of the three, which one depending on `entry.subentries` iteration order, device and entity registry bindings included. A parked subentry is recoverable, a removed one is not.
  * **What this does not fix, named so nobody reads more into it.** Two open remainders and one gap that *is* closed here, kept in the list so the closure is not mistaken for a claim about the whole shape. First, a twin that is left alone keeps its stored `visible_device_ids`; the fix stops the supply, it does not clean up, and the reading side continues to exempt those ids from its in-memory assignment bookkeeping only. Second, `_BaseSubentryFlow._resolve_existing` had the same gap and it is now closed too: its fallback scan asks `_may_answer_for` beside the stored key, so the *handler* side no longer hands a `tracker`-typed subentry storing the service key to `HubSubentryFlowHandler` (nor a `hub`-typed twin on `core_tracking` to the tracker handler). The hand-over branch above the scan is deliberately left unguarded, because that object identifies the subentry the user opened and filtering it would make a mis-typed subentry unreconfigurable; that branch is unreachable today (`async_get_supported_subentry_types` returns `{}` unconditionally, so core never registers these handlers, and `async_step_hub` passes no `subentry`) and is left open so the asymmetry is right the day core does hand one over. What the closure does *not* do is reach a working create where the twin is refused: `ConfigSubentryFlow.async_create_entry` raises `ValueError("Source is None, expected user")` before `async_add_subentry` is called, because `async_step_hub` sets a context without `source`. That raise is **pre-existing** and reproduces on an entry holding no subentry at all, so the axis widened the inputs that reach the branch rather than creating the dead end; it is tracked as `U-28`, and the `unique_id` collision waiting behind it as `U-27`. Third, a **legacy per-account tracker group is still adopted on the reconfigure path when it stands alone**, and both halves of that sentence are measured rather than assumed. The adoption itself reproduces at `b75bea42`, i.e. before this axis existed, because `ConfigEntrySubEntryManager` keys every tracker-typed subentry under `TRACKER_SUBENTRY_KEY`, so `_reset_reconfigure_subentry_context` receives such a group already renamed and never reaches its own type branch. It stops where the **canonical core group sits beside it**: a seed only ranks among candidates that carry the key, and the legacy group is in neither pool, so the canonical group keeps the slot. That half is not cosmetic and was found late: while the seed still outranked the pool, this shape rewrote the legacy group onto the core key and then swept the canonical one, registry bindings included, where `b75bea42` raised `AbortFlow` and kept both. Pinned by `::test_a_seed_that_does_not_carry_the_key_yields_to_the_stored_match` over both insertion orders. Rewriting that branch was tried and reverted: it does not reach the defect and would let a legacy `hub` win the service slot ahead of a real service subentry depending on manager iteration order. The manager fold is where the defect lives, with its own blast radius (runtime index, manager adoption), and it is tracked as `B13` in the register below, owned by `PLAN_GFMY_SUBENTRY_TYPE_MIGRATION`. All three are left to their own change; `tests/test_config_flow_subentry_sync.py::test_a_legacy_tracker_group_on_its_own_key_is_left_alone` therefore pins the migration path alone and says why in its docstring.
  * **Register of the remainders this change carries rather than closes.** Every entry below names four things and is incomplete without all four: the **condition** under which the shape is reachable, the **consequence** a user or a later reader meets, the **date** it was carried, and the **follow-up plan** that owns it. Three of the four are the usual contract discipline; the fourth is here because a remainder whose owner is a plan that has already shipped reads as tracked while nothing tracks it, which is worse than no reference at all. None of these is a defect this change **introduces newly**, and the sharper wording matters because two entries have to defend themselves against the absolute version: for B12 and for U-28 the change shifts or widens the *reachable shape* of a pre-existing gap, and each says so in its own entry. None is a reason to widen this change either: each was measured, each is bounded below, and each is left where its blast radius can be tested on its own. **The register is complete for the remainders of this change and not for the file**: four further remainders share one root of their own -- a group holding ids nothing describes, and a missing allow-list read as "sees everything" -- and are carried together by `PLAN_GFMY_VISIBILITY_ASSIGNMENT_BOOKKEEPING`. That group was `U-25`, `U-26`, `U-30` and `U-31`; `U-26` is **closed** since the normalisation began keeping the empty set for every group (`ffde3fc6`), and `U-25` is **closed** since the branch that synthesises a missing tracker subentry began excluding what another group owns (`989e8634`), so the open set is `U-30`, `U-31` and, newly, `U-34` and `U-35`. Both closures name a commit *and* their pull request, #1235: this repository merges branches rather than squashing them, so the hashes survive, but the pull request is what a reader still finds if that practice changes. The same change also closed a **producer** of the fail-open direction that never had a `U-` name here because it lives in the runtime contract: the setup sync deleted every stored field its definition did not carry (`0ce87ae2`); it is described under "A definition is a partial description" in `agents/runtime_patterns/AGENTS.md`, together with the two further deletion sites of that change and the one deletion that is a repair rather than a defect. **`U-34`**, stated to the same four-part discipline as the entries below: *condition* -- **any** group that stores no `visible_device_ids` key at all, the default one included; *consequence* -- it is read as unrestricted and therefore sees every device, including those another group owns through a non-empty stored list, and the unassigned-device merge does **not** cushion this the way it cushions an empty stored list, because an absent key never restricts in the first place (`::test_ap3_a_stored_shape_decides_what_a_group_may_see[E2]` pins both readings side by side); *date* -- 2026-08-06, measured while closing `U-26`; *owner* -- `PLAN_GFMY_VISIBILITY_ASSIGNMENT_BOOKKEEPING`, which carries it deliberately rather than closing it here, because the question it raises is whether *unrestricted* should exclude foreign ownership, and that is decided where `allow_filter is None` is **read**, not where it is set. **`U-35`**, to the same discipline: *condition* -- any group whose stored list normalises to nothing, that is `[]` or a list of unusable entries, once a coordinator refresh has run; *consequence* -- the refresh writes a derived list back through `ConfigEntrySubEntryManager.update_visible_device_ids`, which levels the stored shape. For a non-tracker group the stored value becomes `[]`, so the count in `_record_allow_list_reinterpretation` reads `0` on every later emission and a migration artefact becomes indistinguishable from a deliberate emptying, which is precisely the distinction that record exists for. For the **tracker** group the write-back is wider still: the unassigned-device merge hands it every device no other group owns, and that becomes its new stored list, so a tracker group cannot be left empty by any route -- the config-flow fix keeps the empty list past *that* writer, not past this one; *date* -- 2026-08-06, measured while the contracts were being caught up; *owner* -- `PLAN_GFMY_VISIBILITY_ASSIGNMENT_BOOKKEEPING`. *Why not here:* closing it means deciding what the write-back should persist for a group whose computed view is empty, and for the tracker group that collides head-on with the "assigned to nobody belongs to the tracker" rule the merge implements. That is a semantic question about the merge, not a missing guard. They are listed here by name rather than by entry so that "the register" is not read as a claim about everything open in this module. **Order of the entries below**, since it is neither numeric nor document order and a register lives on being findable: the four remainders of the type axis first, in the order the work package listed them (`B12`, `B13`, `B10`, `B9`), then the flow-path pair `U-28`/`U-27`, then the dead resolver `U-29`.
    * **B12 -- a lone `hub` adopted into the service slot leaves the entry with no `service`-typed subentry.** *Condition:* a single `hub`-typed subentry wins the service slot (the *Folding renames a `hub`* paragraph further above, not the register intro directly above this line), and the entry holds no `service`-typed subentry beside it. *Consequence:* `known_ids_for_subentry_type` matches `subentry_type` literally, so it returns an empty set for `SUBENTRY_TYPE_SERVICE`; `ensure_config_subentry_id` then finds `valid_ids` empty, skips its membership test and passes the candidate through. It stops verifying rather than breaking *here*, and the platform proceeds in **both** halves of the branch, which is the part worth spelling out because two earlier drafts of this line got it wrong in opposite directions. With a candidate supplied, the empty set skips the membership test and hands it through unchecked. Without one, the helper does return `None` -- but each of the three callers that pass a **service-typed** set carries an explicit `if not service_ids` branch that then *synthesises* an id rather than deferring: `sensor.py` and `button.py` fall back through the forwarded id to the literal `SERVICE_SUBENTRY_KEY`, `binary_sensor.py` to `scope.identifier or scope.subentry_key`. (`device_tracker.py` looks similar and is **not** a case of this: it guards on `known_config_subentry_ids`, the unfiltered set, which the hub still populates in this shape.) So the value that reaches `async_add_entities` is unverified in one half and invented in the other, and the core is where an id the entry does not hold is finally refused: `entity_platform` raises `HomeAssistantError("Can't add entities to unknown subentry …")`, measured in the installed core rather than assumed, and it is a `HomeAssistantError`, not a `ValueError`. A shifted gap either way, not a new one, since `b75bea42` left the same entry without a `tracker` type instead. *Carried:* 2026-08-03. *Follow-up plan:* `PLAN_GFMY_SUBENTRY_TYPE_MIGRATION`. *Why not here:* closing it means giving the hub the service **type**, and `async_update_subentry` does not change a `subentry_type`; the only core route is `async_remove_subentry` plus `async_add_subentry`, and the removal clears the device and entity registry bindings, so this is a migration with its own data-loss question rather than an edit. Widening `known_ids_for_subentry_type` instead was considered and refused: `const.py` and the reading side both state that the entity platforms match the type literally, so a third semantics of the type axis is exactly the drift the shared set forbids.
    * **B13 -- a legacy per-account tracker group is still adopted on the reconfigure path when it stands alone.** *Condition:* one `tracker`-typed subentry storing an email-style `group_key`, with no canonical `TRACKER_SUBENTRY_KEY` sibling in the entry. *Consequence:* `ConfigEntrySubEntryManager._refresh_from_entry` canonicalises every tracker-typed subentry onto `TRACKER_SUBENTRY_KEY` unconditionally, so `_reset_reconfigure_subentry_context` receives the group already renamed and never reaches its own type branch: the legacy group is seeded as the core tracking group, keys and titles included. Reproduces at `b75bea42`, i.e. before this axis existed. Where the canonical core group sits beside it the adoption stops, because a seed only ranks among candidates that carry the key. *Carried:* 2026-08-03. *Follow-up plan:* `PLAN_GFMY_SUBENTRY_TYPE_MIGRATION`. *Why not here:* rewriting the reconfigure branch was tried and reverted -- it does not reach the defect and would let a legacy `hub` win the service slot ahead of a real service subentry depending on manager iteration order. The manager fold is where the defect lives, and its blast radius (runtime index, manager adoption) is a change of its own.
    * **B10 -- the manager leaves a `hub` on its stored key, so `managed_subentries` cannot be read by key alone.** *Condition:* any `hub`-typed subentry that stores a `group_key`. *Consequence:* `_refresh_from_entry` canonicalises `service` and `tracker` by type but falls through for every other type that already stores a key, so a hub keeps whatever it stored and can hold a core key uncontested in that mapping. Anyone reading a subentry out of `managed_subentries` **by key** must therefore apply the type check themselves; the visibility write-back in `async_setup_entry` calls `async_update_subentry` directly and does exactly that, because the guard inside `update_visible_device_ids` cannot see it. *Carried:* 2026-08-03. *Follow-up plan:* `PLAN_GFMY_SUBENTRY_TYPE_MIGRATION`. *Why not here:* the asymmetry is the manager's fold, the same one B13 turns on; changing it moves both at once and needs the migration question answered first. Until then the asymmetry is named rather than removed, which is why the paragraph on the reading side states it as a rule for callers.
    * **B9 -- the tracker group is recognised on one axis, so a legacy tracker key is not recognised at all.** *Condition:* a `tracker`-typed subentry whose stored key is neither `TRACKER_SUBENTRY_KEY` nor the service key -- an email-style legacy key -- reaching a form that preselects a group. *Consequence:* `_default_subentry_key` tests `(stored_key or key)` against `TRACKER_SUBENTRY_KEY`, so it does not recognise that group; where nothing is parked on the tracker key the helper falls through to `next(iter(choices), TRACKER_SUBENTRY_KEY)` and preselects whichever option sorts first by label. Only the *refusal* reads both axes; recognition stays one-axis on purpose, because the second loop that reads both is bound to the parked case and running it unconditionally moved the preselection of `async_step_settings` and `async_step_credentials` away from the service group those two legitimately target. *Carried:* 2026-08-03. *Follow-up plan:* `PLAN_GFMY_SUBENTRY_TYPE_MIGRATION`. *Why not here:* the fix that recognises such a group needs a canonical answer for what the legacy key *is*, which is the migration question again; guessing it in the form would move the preselection for every legacy installation, which is the class the parked-case boundary was drawn to avoid.
    * **U-28 and U-27 -- the create branch of the subentry flows does not complete, and the order of the two abort reasons matters.** *Condition:* `_BaseSubentryFlow._resolve_existing` returns `None` inside `_BaseSubentryFlow.async_step_user` (the name is threefold in this file, so it needs its class), and the update becomes a create. The type axis is **one** route there, namely the sole key-matching candidate being rejected; an entry holding no matching subentry at all is another and is the pre-existing one. Stating only the first would send a reader building a reproduction after an elaborate mis-typed fixture where an empty entry suffices. *Consequence:* `ConfigSubentryFlow.async_create_entry` raises `ValueError("Source is None, expected user")` before `async_add_subentry` is reached, because `async_step_hub` sets a context without `source` (U-28). Behind that raise waits the `unique_id` collision `async_add_subentry` would report as `AbortFlow("already_configured")` (U-27), which is therefore invisible until U-28 is closed. U-28 is **pre-existing**: the same call raises on a config entry holding no subentry whatsoever, so the axis widened the set of inputs that reach the branch rather than creating the dead end. It is also **reachable by an ordinary user**, and the reason is worth stating because it is easy to get backwards: `async_get_supported_subentry_types` returning `{}` closes only the *hand-over* branch, since core then hands over no `subentry` object. The create branch does not depend on it, because `async_step_hub` instantiates `HubSubentryFlowHandler` directly and bypasses the subentry flow manager on purpose, as the comment there says. *Carried:* 2026-08-03. *Follow-up plan:* `PLAN_GFMY_SUBENTRY_FLOW_CREATE_PATH`, U-28 before U-27. *Why not here:* U-28 is a contact point with the flow manager (either the context gains a `source` or the create path is avoided) and needs the core's flow semantics settled first; U-27 additionally needs `_claim_unique_id` lifted to module level, and the three subentry flows carry no `abort` strings at all, so the reason would surface untranslated.
    * **B23 -- a same-typed loser on a foreign key now loses in both load orders, not one.** *Condition:* two subentries of the **same** `subentry_type` share a `unique_id` while storing different `group_key`s, and exactly one of them literally owns its stored key per `LITERAL_CORE_KEY_OWNER` (the reachable shape: a canonically keyed tracker beside a legacy per-account tracker group carrying the same identifier). *Consequence:* the type field added to `_select_canonical` in PR #1236 decides where load order used to, so the non-owner is removed in **both** orders instead of surviving one, and `async_remove_subentry` takes its device and entity registry bindings with it -- while this file states above that several tracker groups with distinct keys are a supported shape. Measured exhaustively over all 19 683 three-subentry shapes: 6 978 remove less than before, 12 remove more in total, 548 remove at least one previously surviving id, and **0** of those own their key literally. *Carried:* 2026-08-07. *Owner:* `PLAN_GFMY_SUBENTRY_DELETION_TYPE_AXIS`. *Why carried:* guarding on the full `(group_key, subentry_type)` identity makes the `unique_id` axis a strict subset of the group axis -- measured: 0 removals it would still contribute -- which switches identifier-collision cleanup off entirely, and "spare a collision, rename it later" is not the same trade as "never clean one up".
    * **B24 -- two `hub`-typed subentries sharing an identifier are still decided by load order.** *Condition:* two subentries typed `hub` share a `unique_id`. *Consequence:* `LITERAL_CORE_KEY_OWNER` maps no key onto `hub`, so both take the same value in the type field, the field ties, the removal guard spares neither (their types match), and the surviving group is whichever the entry yielded first, registry bindings of the other included. *Carried:* 2026-08-07. *Owner:* `PLAN_GFMY_SUBENTRY_DELETION_TYPE_AXIS`. *Why carried:* pre-existing and unchanged by PR #1236 -- measured in both load orders against the merge base and the branch head, identical results -- so closing it is a decision about the hub type itself, which `PLAN_GFMY_SUBENTRY_TYPE_MIGRATION` owns.
    * **U-29 -- a third key-only resolver exists and has no caller.** *Condition:* none today. `ConfigFlow._lookup_subentry` carries exactly the shape this change closed -- scan `entry.subentries`, compare `group_key` alone, return the first match -- and it has **no caller anywhere in the tree**. Note the owning class: it is `ConfigFlow`, *not* `_BaseSubentryFlow`, which only begins below it; an earlier version of this line and the docstring it was copied from both said the latter, and a reader who greps for the wrong owner concludes the register is stale. The claim is stated as "no caller" rather than as a hit count on purpose: a hit count ages the moment anyone mentions the name in prose, and this very entry added such a mention. The reproducible form is `grep -rn "_lookup_subentry" --include=*.py --include=*.md .` outside `.venv` (both filters, or this line is invisible to the very check that is supposed to find it), then reading each hit for a *call* rather than a mention. *Consequence:* no behaviour, but a sleeping instance of the class. It also makes any unqualified "both resolvers" count wrong; the one such statement in the tree, in the docstring of the module-level `_may_answer_for`, already says "both **live** resolvers" and is therefore correct. *Carried:* 2026-08-03. *Follow-up plan:* `PLAN_GFMY_SUBENTRY_FLOW_CREATE_PATH`. *Why not here:* guarding it would add a line no test executes, and removing production code is a change of its own that moves the coverage baseline this branch is measured against.
  * Assurances: `tests/test_config_flow_subentry_sync.py::test_a_non_device_twin_storing_the_tracker_key_never_receives_devices` (both types, both paths) and `::test_a_tracker_typed_subentry_storing_the_service_key_keeps_its_assignment` (both paths). Each runs against an entry double whose `subentries` is a `MappingProxyType`, per `tests/AGENTS.md`. One limit of all of them, stated because it bounds what they prove: `tests/conftest.py` replaces `homeassistant.config_entries` wholesale, so they run against the stub's guard and its mutable `ConfigSubentry`, not against the core's. The collision semantics they rely on were read in the installed core instead (`_raise_if_subentry_unique_id_exists`, called unconditionally from `async_add_subentry` and on change from `async_update_subentry`).
  * Use `_accepts_device_assignment` rather than comparing against `SERVICE_SUBENTRY_KEY` by hand. It reads **both** axes, the stored `group_key` and `subentry_type`, because the alias rule above lets a legacy subentry keep an email-style `group_key` while being typed `service`; a key-only check would miss exactly that subentry.
    * **Which field carries the key axis, because `_SubentryOption` has two and they answer different questions.** The axis reads `stored_key`, never `key`. `key` is an *identity*: the injectivity rule in the next bullet rewrites it to the `subentry_id` of **every** option as soon as any one key is duplicated, which strips its meaning. A predicate that keeps reading `key` for meaning therefore answers correctly only on entries that happen to have no duplicate — and the duplicate that breaks it is the ordinary one, a real `service` subentry sitting beside a legacy `tracker` that also stores `SERVICE_SUBENTRY_KEY`. That parked tracker then passed the key axis, passed the type axis (it *is* a tracker) and became an independently assignable target, while `coordinator/subentry.py` kept indexing it under the stored service key and forcing its visible ids to `()`: the user picks it, the flow reports success, the devices land in no group. Measured, not read, and the dating needs two commits rather than one: the flow logic offers it from `7c799592` (the rewrite) and still at `5a70d157`, while its parent `e8114585` refuses it — but at both of those stands `_gather_subentry_options` still guarded on `isinstance(subentries, dict)`, and a real `ConfigEntry` hands out a `MappingProxyType`, so against the production shape nothing was gathered there at all. The defect is therefore **latent from `7c799592` and production-reachable from `52c72055`** (the `Mapping` guard); both are inside `4c673f32..5a70d157`, so it is PR-introduced rather than inherited either way. Pinned in both iteration orders by `tests/test_options_flow_subentries.py::test_a_tracker_parked_on_the_service_key_is_not_offered_as_a_target`; the second order is cheap insurance rather than a second measurement, because `_gather_subentry_options` ends in `options.sort` by label and is order-invariant today.
    * `stored_key` is `None` exactly where there is nothing stored to judge (a synthesised option, or a subentry without a `group_key`), and the axis then falls back to `key`, which in those cases is a `subentry_id` or the tracker key and never reserved. Put differently, `(stored_key or key)` is precisely the value `key` would have had *without* the rewrite, which is why the axis is rewrite-invariant and why no other case can change its answer. That fallback is unreachable through `_gather_subentry_options` and is kept as a guard for the dataclass's other construction sites, pinned by `::test_an_option_without_a_stored_key_is_still_judged_by_its_key` so it is not decoration.
    * **The predicate is not the only reader that asks `key` for meaning, so do not read the fix as exhaustive.** `_default_subentry_key` answers "which group does the form preselect" with a membership test for `TRACKER_SUBENTRY_KEY`, which the rewrite disarms the same way: no key matches, the helper falls through to `next(iter(choices))`, and a user who submits `async_step_repairs_move` without touching the target field writes the devices to whichever group sorts first by label. Same class, same introducing commit, fixed in the same change: the helper now takes the `option_map` its callers already hold and prefers the option whose *stored* key is the tracker one, pinned by `::test_the_preselected_group_survives_the_collision_rewrite`. **The first attempt at that fix repeated the mistake on the other axis and is worth recording, because the rule above says exactly why.** Preferring the stored tracker key alone is still a hand-written key comparison, so a `service`-typed subentry parked on the tracker key won the preselection over the group that actually holds devices, and both callers passing the *unfiltered* `_subentry_choice_map` (`async_step_settings`, `async_step_credentials`) reach it. **What the type axis buys there is an identity proxy, not damage prevention, and an earlier version of this bullet argued the other one.** It said the harm was that feature-flag writes and title refreshes land on a group that cannot hold devices — which contradicts the `_subentry_choice_map` bullet further down, where the service group is named as a *legitimate* target of exactly those two steps. The harm is narrower and survives that objection: the rewrite took the tracker group's identity out of the key, and `_accepts_device_assignment` is the closest stand-in left, because a group wearing the tracker key while refusing devices is not the group the form means to offer first, whatever else it is. **Only those two call sites reach the second loop at all**, which bounds what it is worth rather than arguing it away: the other three pass `_device_target_choice_map`, whose filter is that very predicate, so a parked group never arrives there. Recognition of the tracker group stays **one-axis** (`stored_key or key` against `TRACKER_SUBENTRY_KEY`); only the refusal reads both, so a legacy tracker on an email-style key is not recognised here — unchanged by this fix and carried as `B9` in the register above, owned by `PLAN_GFMY_SUBENTRY_TYPE_MIGRATION`. Guarding one branch only moved the wrong answer to the next, so a second loop prefers any option that accepts devices; without it the parked subentry still wins by insertion order whenever no collision has rewritten the keys, which is the one case here that is **pre-existing rather than PR-introduced** (without a rewrite the old literal test matched it just as the new loop does, measured at `5a70d157`). Pinned in both shapes by `::test_the_preselected_group_is_judged_on_both_axes`, whose `plain` and `collision` cases have different provenance and are stated as such. **That second loop is bound to the parked case rather than run unconditionally, and the boundary is load-bearing rather than tidiness.** Run unconditionally it also fires on an ordinary legacy entry — an email-keyed tracker group beside a real service group, nothing parked at all — where the pre-fix answer was `next(iter(choices))`; it then moved the preselection of `async_step_settings` and `async_step_credentials` away from the service group those two legitimately target, for every such installation and in both label orders. Measured, and pinned by `::test_the_preselection_is_unchanged_where_nothing_is_parked`, whose second parametrisation is declared as cheap insurance because label order makes both answers coincide there. `option_map` is a required parameter for the same reason: the literal branch existed only for callers that passed no map, none of the five does, and an optional map would hand a forgetful future caller the pre-fix behaviour back on both axes. Each remaining guard has its own killing mutation, so none is decorative. Both loops also skip a key that has no option behind it, rather than the first skipping and the second returning it: an unjudgeable choice must not outrank a judgeable one, and the shape is a guard rather than a live path because all five call sites build `choices` from the map they pass (`::test_a_choice_without_an_option_never_wins_the_preselection`). The remaining readers of `option.key` are identity lookups (the label and option maps, the `option.key == target_key` resolution and the `changed` set), plus two that fire only when nothing is stored (`data.setdefault("group_key", …)` and the title helper's `data.get("group_key") or …`), where the rewrite is a no-op by construction. **One reader is neither, and omitting it from that list was an oversight:** `_gather_subentry_options` derives a display label from `key.replace("_", " ").title()` for a subentry that has neither a title nor an `entry_title`, which reads the key's *content* and would render a `subentry_id` after the rewrite. It is harmless only because it runs in the per-subentry loop *before* the rewrite — an ordering guarantee the inline comment states ("the label the user reads is unaffected either way") but this contract did not, so a future change that moved the rewrite earlier or derived labels lazily would surface opaque labels without touching a single predicate. `_unclaimed_fallback_key` was listed here as an identity lookup and that was wrong on the caller's side: the *helper* only compares, but the `taken` set it is handed decides its answer, so it needs both axes — see the fallback bullet below. A new reader that compares `key` against a *named* key belongs to this class and needs `stored_key`.
  * **The key returned by `_gather_subentry_options` must be injective, and a predicate decided per option must be consumed per option.** Both halves are load-bearing and neither replaces the other. The stored `group_key` is not injective — the alias rule above is exactly what allows one legacy label to sit on a service and a tracker subentry at once — so as soon as **one** duplicate exists, `_gather_subentry_options` moves **every** option onto its `subentry_id`. The all-or-nothing shape is deliberate and the narrower rule is a trap: moving only the duplicates is not total, because a third subentry may store the very `subentry_id` a duplicate is about to take, and chaining that defeats any fixed number of passes. `subentry_id` is the key under which `entry.subentries` stores the subentry, so one pass over all options is injective by construction, and it avoids picking a winner, which would leave the outcome dependent on the `options.sort` by label. Downstream, `_subentry_choice_map` and `_device_target_choice_map` collapse the key into a `dict` and `async_step_repairs_delete` resolves the deletion target and its inherited devices through that mapping, so a duplicate key silently hides one subentry and can delete the other. `_async_assign_devices_to_subentry` additionally compares `option is target_option` rather than the key, which keeps the fan-out onto a non-assignable twin impossible even if the first half regresses.
  * **What that injectivity does *not* buy, stated so nobody reads more into it.** It is flow-local, and the runtime side is now carried by its own rule rather than by this one: `coordinator/subentry.py::_refresh_subentry_index` indexes a subentry of any `NON_DEVICE_SUBENTRY_TYPES` type under `SERVICE_SUBENTRY_KEY` whatever it stores, so the twins no longer collapse onto one slot and the service group can no longer be described by a synthesised placeholder while the real subentry sits under the tracker key. Three further points, named rather than implied. First, folding alone would have **stranded** the devices such a twin accumulated: the service branch forces its visible ids to empty while `stored_assigned_ids` would still count them as assigned, so the unassigned-device merge would leave them in no group at all and the entities would go unavailable. The fold therefore exempts those ids from the *in-memory* assignment bookkeeping (the stored subentry is untouched, and so is this group's own allow-list), and the merge reclaims them into the tracker group. They were never a deliberate move onto the service group in the first place, because the flows do not offer such a subentry as a target. Second, that exemption stays **bound to the fold**, which is not a detail: a subentry already storing the canonical key is a different case, because a device sitting there may be a move the user made while the service group was still an offered target, and `test_a_device_moved_to_the_service_subentry_is_left_alone` pins that it must not be reclaimed. Only the *mis-keyed* ids are residue. Third, the **stored** `group_key` of a mistyped subentry is not migrated by any of this. Where a key is ambiguous the manager writes nothing for it, so that group receives no visibility write-back; its metadata still carries the devices, so this is inert rather than invisible, which is the direction this pair fails towards.
  * **The synthesised fallback must not borrow an identity, and a move needs a real destination.** When no group accepts devices, `_device_target_choice_map` synthesises an option. Taking `TRACKER_SUBENTRY_KEY` outright borrowed a key: an entry whose only subentry is typed `service` but stores `core_tracking` was filtered out by its type, the fallback offered *its* key back, `_async_assign_devices_to_subentry` resolved that key against the **unfiltered** set, found the real non-assignable subentry and refused — and `async_step_repairs_move` mapped the empty result onto `subentry_move_success`, telling the user a device moved that did not. Measured. Three points now carry the repair, and each is needed:
    * `_unclaimed_fallback_key` picks a key no option holds. The search walks until it is free rather than trying one substitute, because a second subentry may store the very substitute the first is displaced to. Note that the two `if not options:` guards are **not** the same test: the one in `_gather_subentry_options` fires on the unfiltered list, where nothing exists that could hold the key, while this one fires on the filtered list with a possibly non-empty unfiltered set. Only the second can borrow. **The `taken` set carries both axes, and that is the caller's job rather than the helper's.** Handing it `{option.key …}` alone was correct until the injectivity rewrite below turned every key into a `subentry_id`: the set then holds no stored `group_key` at all, and the helper hands back `core_tracking` while a real subentry stores it — the borrow it exists to prevent, restored by the very rule that made the keys injective. Same class and same introducing commit as the two readers above, so it is closed with them; pinned by `::test_the_synthesised_fallback_does_not_borrow_a_rewritten_key`. No live loss follows today because `_async_assign_devices_to_subentry` resolves by option identity, which is the next bullet and exactly why the borrow must not be left to it alone.
    * `_async_assign_devices_to_subentry` stands down when *no* option carries the target, not just when a non-assignable one does. Its `else` branch strips the ids from every group that holds them; with a real target that is the second half of a move, without one it is a loss — and it would be reported as success, because a strip fills `changed`.
    * `async_step_repairs_move` drops the synthesised fallback from its target field, mirroring the `real_fallback_keys` line `async_step_repairs_delete` already draws, and aborts on the existing `repairs_no_subentries`. The earlier note claimed the honest repair needed a new abort string; that was wrong, the existing one carries it and `translations/` stays untouched. `async_step_visibility` keeps the fallback on purpose: there the request is to leave the ignore list, which the option write and the reload carry, and an id no subentry claims is merged into the tracker group by `coordinator/subentry.py`.
  * Narrow only the steps that assign devices. `_subentry_choice_map` fed all six steps before the split and still feeds four of them, each of which must keep seeing every group: `async_step_settings`, `async_step_credentials`, `async_step_repairs` (which only asks whether *any* subentry exists, so a shared filter could send an entry whose sole subentry is the service group into `repairs_no_subentries`) and the `removable_choices` of `async_step_repairs_delete`. For the same reason `_device_target_choice_map` keeps the `core_tracking` fallback of `_gather_subentry_options`: filtering must never hand `vol.In` an empty mapping.
  * Deletion is a separate question from assignment, but not an independent one. `async_step_repairs_delete` keeps its unfiltered `removable_choices`, so the service group stays removable; only the fallback that inherits the deleted group's devices is narrowed. The two sides are coupled through `fallback_key != target_key`, so a group is only offered for deletion while a **different** group with a real backing subentry can inherit its devices. Skipping that re-derivation left the common shape — one tracker group plus one service group — with a fallback field whose single value was always the deletion target, so every submission failed on `invalid_subentry`.
* **The reading side enforces the same rule for the canonical service key, so the two move together there.** In `coordinator/subentry.py::_refresh_subentry_index`, the branch on `group_key == SERVICE_SUBENTRY_KEY` forces `visible_ids`, `enabled_ids` and `manager_visible_ids` back to `()`. That branch still tests one key, but the key it tests is now type-authoritative in **both** directions: a subentry of any type in `NON_DEVICE_SUBENTRY_TYPES` is folded onto `SERVICE_SUBENTRY_KEY` before it, so the branch catches the email-style twin that a key-only test used to miss, and a `tracker` storing that key is folded away from it, so the branch no longer empties the ids of a group that bears devices. The type set is imported from `const.py` rather than restated, so the side that refuses such a group as an assignment target and the side that indexes it cannot drift apart, and `hub` belongs in it for the same reason `service` does: `HubSubentryFlowHandler` sets `_group_key = SERVICE_SUBENTRY_KEY` and the service feature platforms, so a hub *is* the service group under a second entry point — **for the assignment predicate and this index**, not everywhere: the entity platforms still key off the literal type (`known_ids_for_subentry_type` matches `== "service"`), so a hub appears in neither the service nor the tracker id set there. A legacy hub carrying a tracker key, an email-style key or no key at all used to keep a device-bearing slot and receive the write-back; only the tracker-key shape *additionally* overwrote the tracker's metadata, the other two open a group of their own. All three shapes and both iteration orders are pinned in `tests/test_coordinator_subentry_visibility.py`; the order axis is load-bearing for the tracker-key shape alone.

  Two asymmetries follow from this and are named rather than left to be rediscovered. First, `ConfigEntrySubEntryManager._refresh_from_entry` canonicalises `service` and `tracker` by type but leaves `hub` on its **stored** key, so the two sides deliberately diverge: anyone reading a subentry out of `managed_subentries` **by key** must apply the type check themselves. The visibility write-back in `async_setup_entry` calls `async_update_subentry` directly and therefore does exactly that, because the guard inside `update_visible_device_ids` cannot see it. Second, `core_group_keys_present` keeps reading the stored key (see the deliberate exception below), and it now covers **two** shapes rather than one: a folded `hub` reports the service key as present while the index describes that group with a synthesised placeholder, and a parked `tracker` reports the service key as present while the index has moved it to `core_tracking`. Both are inert rather than lossy, and the second by its own mechanism rather than by analogy -- its ids are exempted from the in-memory assignment bookkeeping, so the merge collects the devices into the tracker group. Tracker subentries keep their stored key wherever that key names a group of its own, because several tracker groups with distinct keys are a supported shape; the parked-on-the-service-key shape is the one exception. Several subentries can end up answering for the service key at once, and which one describes it is decided by a **rank, not by iteration order**. Two shapes reach that point today: the repair path creates a canonically keyed service subentry while a mis-keyed twin is still on disk; and a `hub` stores the service key *by design*, a shape the cleanup above now deliberately preserves rather than sweeping. A `tracker` parked on that key was a third until PR #1230: it survives the sweep (`::test_a_tracker_parked_on_the_service_key_is_not_swept_up`) and competed here, winning on the exact field. PR #1230 removed it from this key by folding it onto `TRACKER_SUBENTRY_KEY` before the rank, which is also what closed the `_may_answer_for` asymmetry named further down: both sides now keep that shape out of the service pool. The ordering criteria are the ones `_resolve_existing` applies in `config_flow.py`, and their agreement is enforced by a shared definition rather than by convention: `LITERAL_CORE_KEY_OWNER` lives in `const.py` and all three sides import it (the flow, this index and `ConfigEntrySubEntryManager._candidate_score`), because a slot won on the flow side and lost here would rebind the service device to a subentry the entity platforms do not select (they match `subentry_type == "service"` literally). The manager was the last to join: it resolved key collisions without reading `subentry_type` at all until PR #1230, so a hub could take a core slot by iteration order. It now ranks exact stored key, then literal owner, then the lowest identifier, spelled "higher wins" against this side's "lower wins". Its tuple carries an `subentry_id_present` field between the second and the tie-break, but that field cannot discriminate at its only call site: `_select_preferred_managed` passes both operands through `_is_subentry_like` first, which already demands a non-empty `str` id. The contract criterion "a candidate whose identifier is missing sorts last" is enforced there, by the guard, not by the tuple, and the field is named here only so the next reader does not count it as a working third criterion. One thing stays asymmetric and is named rather than left to be rediscovered: its **fold** is unchanged, so a `hub` still keeps its stored key and can hold a core key uncontested (carried as `B10` in the register above, owned by `PLAN_GFMY_SUBENTRY_TYPE_MIGRATION`). A second asymmetry existed until PR #1230 and is recorded because the argument that preserved it was wrong in a specific way. The manager carried two provenance fields (`entry_match`, `unique_match`) *behind* those three, which have no counterpart here, and "behind" was taken to make them harmless. It did not: sitting between "identifier presence" and the lowest-identifier tie-break, `unique_match` decided pairs the contract wanted decided by id. On `(id-a-no-uid, unique_id=None)` against `(id-z-with-uid, "<entry_id>-core_tracking")` the manager took the higher identifier in **both** iteration orders while this side and the flow take the lower one, and `_refresh_subentry_index` then hands one holder's visible list to a manager naming the other. The reachability argument that kept the field read "it takes a subentry `unique_id` without the entry id, which no writer in `custom_components/` produces" — true of a wrongly *shaped* identifier, irrelevant to a `None`, which needs no writer at all: the core builds subentries with `subentry_data.get("unique_id")` in `ConfigEntry.__init__` and defaults the argument of `ConfigSubentryFlow.async_create_entry` to `None`. How long that shape lasts is a **window**, not a permanent state, and an earlier draft of this sentence said "never repaired": the subentry write-backs at `config_flow.py:8331`/`:8373`/`:8478` pass an existing value through unchanged, but `_async_sync_feature_subentries` assigns `f"{entry_id}-{key}"` to the core subentry it resolved (`:7303`/`:7329`, and `_claim_unique_id` at `:7230`), so the next options-flow pass normally repairs it. Entry load to next flow run is long enough for the runtime index and the visibility write-back to run inside it. Both fields are removed; the manager's tuple is the three shared fields, and the tie-break is now reached by every pair they leave equal. Pinned in both orders by `::test_ap8_the_manager_and_the_index_agree_when_one_identifier_is_missing`; its killing mutation is restoring `unique_match`. Restoring `entry_match` alone leaves it green, which is the measurement rather than a gap — that field is uniformly `0` because `ConfigSubentry` declares no `entry_id`, so it was removed for being inert, and the "each field has its own killing mutation" rule above does not reach a field that cannot discriminate. **Three of four, and the fourth runs first:** on that same pair `_deduplicate_subentries`'s `_select_canonical` groups by `(group_key, subentry_type)`, sorts a *present* `unique_id` first, and so keeps the higher identifier and **removes** the lower one — the subentry the other three now name — with `async_sync` calling it unconditionally before anything else. That removal is pre-existing (`B16`, owned by `PLAN_GFMY_SUBENTRY_DELETION_TYPE_AXIS`, where the input shape is measured); what changed is which side of it the manager stands on. Describing that helper as deciding "by iteration order" is the shorthand to avoid: the index is only its *fourth* sort field, and PR #1236 is why it is fourth rather than third. That PR gave `_select_canonical` a type field ranked against the same `LITERAL_CORE_KEY_OWNER`, sitting between the identifier and the index, because on the **`unique_id` axis** everything above it ties by construction — the candidates were grouped *by* `unique_id` — so the index decided alone and the survivor was whichever subentry the entry happened to yield first. The production shape reaching that axis is a `hub` and a `service` sharing `f"{entry_id}-service"`, and in one of the two load orders the removal took the canonical service group, registry bindings included: the same defect this file records above for the flow side. **The rank alone was not the fix, and the correction is worth the sentence because it repeats a split this file already resolved once.** Ranking made the `hub` the loser in *both* orders, trading a random silent removal for a reliable one -- against the rule stated two bullets above, that a hub losing the slot is a group of its own the cleanup must leave alone. The flow-side sweep had carried that guard since PR #1230; the manager-side deduplicator had only the rank until Codex flagged it on PR #1236. It now also *guards* the removal: within a `unique_id` bucket only a candidate whose `subentry_type` matches the canonical one is removed at all, which is the same "a foreign type is a group of its own" rule the group axis gets for free by carrying the type in its grouping key. **The converse is deliberately not true, and saying so is the point of this sentence:** the guard compares the *type* only, never the stored key, so two candidates of the **same** type sharing an identifier are still collapsed however far apart their `group_key`s are. Where exactly one of them literally owns its stored key, field 3 decides where load order used to, and the other loses in *both* orders rather than one; where **neither** owns its key -- two trackers on two different legacy keys, say -- field 3 ties at 1, load order still decides, and nothing changed at all (measured in both directions, before and after). Measured over all 19 683 three-subentry shapes: 6 978 shapes remove less than before, **12 remove more in total**, 548 remove at least one id that used to survive one order, and **0** of those carry a type owning its stored key literally. Where the loser is a legitimate separate group -- several tracker groups with distinct keys are a supported shape, as this file states above -- that is a real loss, carried as `B23` and owned by `PLAN_GFMY_SUBENTRY_DELETION_TYPE_AXIS`. It is carried rather than closed because guarding on the full `(group_key, subentry_type)` identity would make this axis a strict subset of the group axis, i.e. switch identifier-collision cleanup off entirely, and the deferred-rename trade that justifies sparing a collision is not the same trade as never cleaning one up. Both are load-bearing and each has its own killing mutation: dropping the guard restores the reliable removal (`::test_dedup_keeps_both_a_hub_and_a_service_sharing_one_identifier`, both orders), and neutralising the rank lets a load-order hub win the bucket so that *nothing* matches its type and a genuine duplicate survives with it (`::test_dedup_the_guard_still_needs_the_rank`, which needs three candidates in distinct group buckets to be observable at all). What the guard does **not** do is resolve the shared identifier: `_claim_unique_id` lives in `_async_sync_feature_subentries` and never runs from the manager, so the collision is deferred to the next flow pass rather than cured -- deliberately, because `async_remove_subentry` takes registry bindings with it and a deferred rename does not. The claim that this is free was made once and withdrawn: a spared twin can itself take a core slot through its stored key, and then the write-back *does* change an identifier and *does* reach the core's `_raise_if_subentry_unique_id_exists`, ending in the `_async_adopt_existing_unique_id` fallback after a second abort (reproduced in both directions by `::test_dedup_sparing_a_twin_can_route_a_sync_through_the_adoption_exit`). Both paths end at the same holder, so what is traded is an irreversible removal for a logged fallback. Note also that "left alone" describes this helper: `async_sync`'s own type-blind stale sweep still removes a spared `hub` that stores a legacy or absent key, and only the production shape -- a hub on the service key beside a real service -- is verified to survive a whole sync (`::test_dedup_a_spared_hub_survives_a_whole_sync_not_just_the_helper`). Mind the direction, because it is inverted twice over: `_candidate_score` compares with `>` so the owner ranks *high*, `_select_canonical` picks with `min` so the owner sorts *low*. On the `(group_key, subentry_type)` axis the new field is constant, since the type is part of that axis's grouping key, so everything said above about that axis still holds unchanged. Exact stored key beats a folded twin (the flow expresses that as `pool = exact or folded` rather than as a rank field, to the same effect), the literal owner beats a type that merely folds onto the key, a candidate whose identifier sanitises to `None` sorts last, and only among equals does the lowest `subentry_id` decide, where the value is arbitrary and stability is the whole requirement. **"The same criteria" is deliberately not "field for field", and the two differences that remain are worth naming, together with a third PR #1230 closed.** The flow additionally prefers its seeded candidate, a notion this pass has no equivalent of; and it sorts on the **raw** `subentry_id` where this pass sorts on the sanitised, provisional-filtered one, which is why the missing-identifier field exists here and has no counterpart there. A third sat between them until PR #1230: the flow gates candidates through `_may_answer_for`, so a `tracker` storing the service key was excluded there while it competed on the reading side and won on the exact field. The reading side's fold removes it from that key too, so the two sides agree on the shape instead of disagreeing. The identifier-shape difference can only make the two sides disagree for a shape no production site in `custom_components/` creates (`-provisional` subentry ids: greppable readers, no writer), and it made the disagreement *smaller*, not larger, because the reading side had no rank at all before. Two residues stay named rather than fixed: the tie is resolved in favour of the incumbent (`candidate_rank >= incumbent_rank`), so two candidates whose identifiers *both* normalise away rank equal and the group's title comes from whichever the entry yielded first, and that direction is **deliberately not pinned**, because the shape cannot be produced: `ConfigSubentry.subentry_id` is a `str` defaulting to a fresh ULID, and the only other way to normalise an identifier away is a `-provisional` id, for which the tree has readers but no writer, so a test would model a state the core does not reach; and a subentry whose id is filtered as provisional keeps `config_subentry_id=None` rather than gaining the synthesised placeholder, because `service_provisional_seen` makes the stable-id block skip the key. Each field has its own killing mutation, so none is decorative: neutralising the owner field kills `::test_a_literal_service_subentry_outranks_a_hub_storing_the_same_key` in both orders, the identifier field `::test_two_equally_ranked_service_subentries_resolve_by_identifier`, the missing-identifier field `::test_a_subentry_without_a_usable_identifier_loses_the_service_slot` in both orders, and the exact-key field `::test_the_canonically_keyed_service_subentry_wins_regardless_of_order`. Displacing a weaker holder needs no cleanup, which is a property of this loop rather than a general one, and the reason had to be re-derived when the rank grew to cover the tracker key: the four structures the loop writes divide in two. `metadata` and `manager_visible` are keyed by **group**, so a later winner overwrites the loser's entry instead of needing it removed. `feature_map` (keyed by *feature*, via `setdefault`) and `default_key` (a scalar behind an `is None` guard) are never overwritten at all, and are harmless for a different reason: the value written in both cases is the `group_key` itself, which winner and loser share, so a loser's entry never points at the wrong group. Where the per-key feature constants are empty and each subentry brings its own `data["features"]`, a loser can still widen the `feature_map` key *set* into a union — coarser, not misdirected. For the service key a stronger statement also holds -- `manager_visible` is never filled for it at all -- but that one does *not* generalise: `manager_visible` **is** filled for the tracker key, so the overwrite argument is the one that carries both. Pinned by `::test_ap4_a_displaced_tracker_leaves_no_stale_manager_write`, whose killing mutation is turning the assignment into a `setdefault`. **One structure is filled above the rank and therefore also counts the loser**, named because an earlier draft of this paragraph claimed the list was exhaustive: `stored_assigned_ids` collects every id a subentry claims as its own, regardless of who wins the slot -- *not* every id standing in the in-memory view, since a folded subentry keeps its ids there as its allow-list — not every *stored* id, because a mis-keyed twin and a parked tracker are exempted by their fold, which is how their devices reach the merge. That is not a leak but the mechanism `test_a_device_moved_to_the_service_subentry_is_left_alone` depends on, and its cost is the one the parked-subentry note above describes: ids held by a loser on the canonical key are neither described by a group nor reclaimed by the merge. The rank does not change that either way; it only makes *which* subentry holds the slot deterministic. **The tracker key is ranked by the same block**, which PR #1230 added; before it, two `tracker`-typed subentries storing `core_tracking` resolved by iteration order, measured byte-identical at `bf3a36aa`. All three sides are now symmetric for both core keys: `_resolve_existing` was already parametric in the key (its `min(...)`), `_candidate_score` takes the canonical key as an argument, and the reading side's block covers `service` and `core_tracking`. Each tracker-key field has its own killing mutation, so none is inherited on trust: `::test_ap4_an_exact_tracker_key_outranks_a_parked_twin` for the exact field, `::test_ap4_the_literal_tracker_owner_outranks_a_foreign_type` for the owner field (its reachable shape is an unknown or future type, since `service` and `hub` fold onto the service key before they get there), and `::test_ap4_the_tracker_slot_no_longer_depends_on_iteration_order` for the tie-break. That the manager and the index pick the *same* subentry is asserted separately, in both orders, by `::test_ap4_the_manager_and_the_index_agree_on_the_tracker_slot`; without it a slot won on one side and lost on the other would let the visibility write-back rebind entities silently. One deliberate exception sits above the fold: `core_group_keys_present`, which drives the missing-core-subentry repair, is still collected from the **stored** key, because what it answers is which core groups exist on disk. Moving that read to the canonical key would change which subentry the repair creates, and that is a decision of its own, not a side effect of this one. The same pass collects the ids the folds did not exempt into `stored_assigned_ids` precisely so a device parked on the service group is *not* treated as unassigned and pulled back into the tracker. Anyone changing one side changes the other: relaxing the writer without the reader stores ids the reader refuses to count as an assignment on every refresh, and relaxing the reader without the writer resurrects assignments the flows no longer offer.

### Optional `ConfigEntry` attributes in tests

Local discovery and reconfigure tests instantiate lightweight `ConfigEntry` doubles that frequently omit optional attributes Home Assistant adds at runtime.

* `source` — prefer `getattr(entry, "source", None)` before accessing the field so `async_step_discovery` continues to work with the stubs in `tests/test_config_flow_discovery.py`.
* `pref_disable_new_entities` / `pref_disable_polling` — guard these through `getattr(..., False)` when feature toggles depend on them, because flow helpers in the test suite never populate the preferences block.
* `state` — normalize through `getattr(entry, "state", None)` before checking reload eligibility; the discovery update fixtures only set `entry_id`, `data`, and sometimes `unique_id`.

Add similar guards whenever a new optional attribute becomes relevant so future config flow helpers remain compatible with the suite's minimal stubs.

* **Preserve parent-platform forwarding state across retries.** When a setup, reconfigure, or auth retry path short-circuits the normal flow, retain the `_gfm_parent_platforms_forwarded` flag on `entry.runtime_data` so unload handlers can skip subentry teardowns when parent platforms were never forwarded. This prevents `ValueError: Config entry was never loaded!` noise after partial setups.
* **Reconfigure context markers.** Home Assistant populates `flow.context["entry_id"]` when a reconfigure flow starts (for example via `config_entries.async_start_reconfigure`). Treat that value as authoritative when routing the user step so the flow stays bound to the existing entry instead of tripping duplicate-account guards. Inline comments should call out this guard relaxation to preserve the single-parent-entry rule for new setups while keeping reconfigure detours safe.
* **Manual token UI path disabled.** The `_AUTH_METHOD_INDIVIDUAL` choice in `STEP_USER_DATA_SCHEMA` remains commented out because the manual token + email path is broken. Keep the commented line (with its inline note) intact until the workflow is fixed and ready to re-enable. The options flow credential refresher (`async_step_credentials`) also keeps the manual token input commented out. The reauth form no longer has a manual token field to keep: that surface was removed, see the amendment below.
  * **Removed: the container-login credential fetch (PR #1218, 2026-07).** The existing-entry forms once carried a narrow carve-out from the "secrets-only" wording above: three fields that pulled a freshly minted bundle out of the login container over a host-published port. That transport was unencrypted, so the whole path was removed together with its error keys, its translations and its tests; `tests/test_track_b_removal_guard.py` keeps its vocabulary out of the tree. That carve-out from the "secrets-only" wording is therefore gone; the per-step status of the rule is stated in the manual-token amendment below, because it differs between the two steps. Re-introducing a credential method needs a new amendment here, not a revival of the old one.
  * **Amendment: local-bundle pre-flight is an additional credential path.** `async_step_found_local_bundle` offers a `secrets.json` bundle that was found on disk before the method form is shown. It is a `secrets.json` import, not a new manual-token surface, and it exists because the file watcher is armed in `async_setup`, which never runs on a fresh install without a config entry. The offer is skipped when every bundle found belongs to an account that is already configured (compared through `normalize_email` + `unique_account_id` against existing entry unique IDs), so multi-account setups keep working; declining it returns to the regular method form.
  * **Removed: the manual token reauth branch (PR #1229, 2026-07).** `async_step_reauth_confirm` once accepted a raw OAuth token pasted into the reauth form as a second credential path, in the success case and again in the multi-entry-guard deferral. Its form field was commented out, and `_interpret_reauth_choice` has no `return` that can produce `"manual"` either, so the branch was unreachable from the form **and** from the code. It was removed rather than covered by a test, because a test feeding the method directly would have rebuilt this contract instead of checking it. `tests/test_manual_reauth_removal_guard.py` keeps it out on three axes: lexically, by sweeping `config_flow.py` for the branch vocabulary; structurally, by pinning that `_interpret_reauth_choice` can only hand back `None` or `"secrets"` as its method element; and by wiring, by pinning that the reauth step reads no interpreter other than that one, because `_interpret_credentials_choice` does hand back `"manual"` as a method element and a call site pointed at it would pass the other two axes untouched. That function still *reads* `_REAUTH_FIELD_TOKEN`, so a token which reaches it anyway is rejected with `choose_one` rather than silently ignored while the bundle half proceeds; that read is a rejection, not a path. Reviving a manual reauth surface needs a new amendment here. The two other surfaces named above are untouched by this removal and their commented lines stay, but note that they are *not* in the same state: the `STEP_USER_DATA_SCHEMA` choice only hides a menu entry, whereas `async_step_credentials` keeps a **dormant but live** manual-token branch behind its commented-out field (`config_flow.py`, `if has_token:` in the credential refresher), which still validates and persists an individually pasted token. The "secrets-only" wording further up therefore holds without exception for `async_step_reauth_confirm` after this removal, but for `async_step_credentials` it describes the form, not the code beneath it.
* **Guard fallbacks require secrets-only inputs.** When the multi-entry guard trips inside `async_step_credentials`, rebuild the fallback payload solely from `secrets.json` inputs. That requirement is now checked rather than assumed: the deferral is gated on `has_secrets`, because both credential branches call `_finalize_success` inside the same `try`, so a guard error can also arrive from a submission that carried no bundle at all. That gate is defence in depth, not a fix for a live user path: with the token field commented out, the form cannot submit one (`vol.Schema` defaults to `PREVENT_EXTRA` and the flow manager validates against `data_schema` first), so today only a direct call reaches it. Gate on the *value*, not on key presence: a blank `new_secrets_json` passes the exclusivity count and would reach `json.loads("")`. Avoid reintroducing dormant manual-token branches in the guard handler while the UI path remains disabled so UnboundLocalError-style regressions do not resurface.
* **Token validation exhaustion warning.** When every token candidate fails validation, the flow emits a warning instructing the user to re-enter credentials so expired bundles are refreshed. Keep this log intact (see `_log_token_validation_failure`) so support teams can direct users back through reauthentication instead of silently accepting bad tokens.

## Service validation fallbacks

* When raising `ServiceValidationError`, always include both the translation metadata (`translation_domain`, `translation_key`, and `translation_placeholders`) **and** a sanitized `message` that reuses the same placeholders. This keeps UI translations working while ensuring Home Assistant surfaces a readable fallback when translations are unavailable.

### Fallback verification checklist

1. Run `pytest tests/test_hass_data_layout.py::test_service_no_active_entry_placeholders -q` to confirm placeholder usage remains stable.
2. Add new translation-focused tests alongside updates so each fallback path has coverage.

## Config entry options persistence reminder

* Treat `ConfigEntry.options` as immutable during reconfigure flows. Build a new dictionary (for example, `existing_options = dict(entry.options or {})`) and pass it directly to `async_update_entry` instead of mutating `entry.options` in place. Home Assistant only persists option changes when it detects a new mapping, so keep the original object untouched until `async_update_entry` returns. See the [`async_step_reconfigure` options-copy pattern](../../config_flow.py#L2929-L2943) for a concrete implementation:

  ```python
  existing_options = dict(getattr(entry_for_update, "options", {}) or {})
  existing_options.update(options_payload)
  self.hass.config_entries.async_update_entry(
      entry_for_update,
      data=merged_data,
      options=existing_options,
  )
  ```

## Cross-reference checklist

* [`docs/CONFIG_SUBENTRIES_HANDBOOK.md`](../../../docs/CONFIG_SUBENTRIES_HANDBOOK.md) — Mirrors this guide's subentry-flow reminders and now tracks every AGENT link. Update both documents together whenever setup/unload contracts, discovery affordances, or reconfigure hooks change.

## Subentry handler registration (HA 2026.x compatibility)

### `async_get_supported_subentry_types` MUST return empty dict

**Critical:** This method MUST return `{}` to hide unwanted UI buttons in the config entry panel.

**Why empty dict is required:**
- Returning handler classes causes HA to display "+ Add hub feature group" and "+ Add service feature group" buttons
- These manual subentry buttons should NOT be visible to users
- Subentries are provisioned **programmatically** by the integration coordinator, not manually

**Correct implementation:**
```python
@classmethod
@callback
def async_get_supported_subentry_types(
    cls,
    _config_entry: ConfigEntry,
) -> dict[str, type[ConfigSubentryFlow]]:
    """Return empty dict to hide subentry UI buttons."""
    return {}  # MUST be empty to hide manual add buttons
```

**Wrong implementation (exposes unwanted UI):**
```python
# DON'T DO THIS - exposes "Add hub/service feature group" buttons!
return {
    SUBENTRY_TYPE_HUB: HubSubentryFlowHandler,
    SUBENTRY_TYPE_SERVICE: ServiceSubentryFlowHandler,
}
```

### `async_step_hub` must instantiate handlers directly

Since `async_get_supported_subentry_types` returns empty, the "Add Hub" flow entry point (`async_step_hub`) must instantiate the handler class directly:

```python
async def async_step_hub(self, user_input=None):
    # Don't use async_get_supported_subentry_types - it returns {}
    # Instantiate the handler directly instead
    handler = HubSubentryFlowHandler(config_entry)
    setattr(handler, "hass", hass)
    setattr(handler, "context", {"entry_id": config_entry.entry_id})
    result = handler.async_step_user(user_input)
    return await self._async_resolve_flow_result(result)
```

### Lazy `config_entry` resolution for handler compatibility

Subentry handlers use a `config_entry` property with lazy resolution to support both direct instantiation and potential future HA flow manager usage:

```python
@property
def config_entry(self) -> ConfigEntry:
    if self._config_entry_cache is not None:
        return self._config_entry_cache

    # Fallback: try HA's _get_entry() method
    get_entry_method = getattr(self, "_get_entry", None)
    if callable(get_entry_method):
        entry = get_entry_method()
        if entry is not None:
            self._config_entry_cache = entry
            return entry

    raise RuntimeError("Cannot resolve config_entry")
```

### Test expectations

Tests must verify:
1. `async_get_supported_subentry_types` returns empty dict `{}`
2. `async_step_hub` creates entries successfully (via direct handler instantiation)

