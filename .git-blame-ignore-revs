# git blame ignore revs
#
# Revisions listed here are skipped by `git blame`. Use them for large,
# purely mechanical changes (e.g. repo-wide auto-formatting) so they do
# not obscure the real authorship of each line.
#
# GitHub honors this file automatically in its blame view. Locally, run:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: apply ruff format across repository (ruff 0.14.14)
c9a54060dd1be461bf272d4707d32fd7c6891448
