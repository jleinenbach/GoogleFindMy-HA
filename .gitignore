.truststore/
__pycache__/
pytest_output.log
.coverage
.coverage.*
.wheelhouse/
.bootstrap/
node_modules/
.npm-cache/
*.egg-info/
.hypothesis/
jscpd-report/
coverage.xml
.venv
.plans/

# pip-audit run artifacts. .github/workflows/pip-audit.yml regenerates both
# from poetry.lock on every run and nothing reads a committed copy; a stale
# committed export only feeds Dependabot versions the lock no longer carries.
/poetry-requirements.txt
/audit.json
